import styled from 'styled-components'

const Wrapper = styled.nav`
  .navbar {
    text-align: center;
  }

  .nav-name {
    font-weight: bold;
    font-size: 28px;
    margin-right: 2rem;
  }

  .nav-item .nav-link {
    font-size: 20px;
    font-weight: 750;
  }


`

export default Wrapper